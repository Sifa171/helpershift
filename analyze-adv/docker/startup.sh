#!/bin/bash

# Check if logging is enabled
echo "Started analyze pod ${HOSTNAME}"

# Initialization done
echo "Initialization done, starting loop stay alive"
while true
do
	sleep 60s
done

echo "Loop somehow ended"
